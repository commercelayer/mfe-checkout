import { css } from "styled-components"
import tw from "twin.macro"

export const CheckCss = css`
  ${tw`border-2 border-gray-300 rounded text-primary focus:border-primary focus:ring focus:ring-offset-0 focus:ring-primary-light focus:ring-opacity-50`}
`
